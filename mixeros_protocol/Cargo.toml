[package]
name = "mixeros_protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
mdns-sd = "0.18.2"
bytes = "1"
bincode = "2.0.1"
serde = { version = "1", features = ["derive"] }